<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.woorifisan.bank.domain.account.mapper.TransactionLedgerMapper">
    <select id="findByTxId" resultType="com.woorifisan.bank.domain.account.model.TransactionLedger">
        SELECT * FROM transaction_ledger WHERE tx_id = #{txId}
    </select>

    <insert id="insert">
        INSERT INTO transaction_ledger (tx_id, account_id, tx_type, amount, balance_after, target_bank_code, target_account, description, status, transacted_at)
        VALUES (#{txId}, #{accountId}, #{txType}, #{amount}, #{balanceAfter}, #{targetBankCode}, #{targetAccount}, #{description}, #{status}, #{transactedAt})
    </insert>
</mapper>
