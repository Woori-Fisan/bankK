<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.woorifisan.bank.domain.account.mapper.TransactionLedgerMapper">
    <select id="findByTxId" resultType="TransactionLedger">
        SELECT * FROM transaction_ledger WHERE tx_id = #{txId}
    </select>

    <select id="findHistoryList" resultType="TransactionLedger">
        SELECT * 
        FROM transaction_ledger 
        WHERE account_id = #{accountId}
          AND transacted_at BETWEEN STR_TO_DATE(#{startDate}, '%Y-%m-%d') 
                                AND STR_TO_DATE(CONCAT(#{endDate}, ' 23:59:59'), '%Y-%m-%d %H:%i:%s')
        ORDER BY transacted_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countHistory" resultType="int">
        SELECT COUNT(*) 
        FROM transaction_ledger 
        WHERE account_id = #{accountId}
          AND transacted_at BETWEEN STR_TO_DATE(#{startDate}, '%Y-%m-%d') 
                                AND STR_TO_DATE(CONCAT(#{endDate}, ' 23:59:59'), '%Y-%m-%d %H:%i:%s')
    </select>
</mapper>
