<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.woorifisan.bank.domain.loan.mapper.LoanProductMapper">
    <select id="findByProductId" resultType="LoanProduct">
        SELECT * FROM loan_product WHERE product_id = #{productId}
    </select>
</mapper>
