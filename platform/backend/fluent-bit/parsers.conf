[PARSER]
    Name        json_log
    Format      json
    Time_Key    @timestamp
    Time_Format %Y-%m-%dT%H:%M:%S.%L%z
    # 파싱 후에도 @timestamp 필드를 레코드에 유지 - Off면 내부 시간으로만 쓰고 필드는 삭제됨
    Time_Keep   On
