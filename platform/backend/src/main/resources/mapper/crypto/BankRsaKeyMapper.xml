<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.woorifisan.platform.crypto.mapper.BankRsaKeyMapper">

    <select id="existsByBankCode" parameterType="string" resultType="boolean">
        SELECT CASE WHEN COUNT(bank_code) > 0 THEN 1 ELSE 0 END
        FROM bank
        WHERE bank_code = #{bankCode}
    </select>

    <update id="updateBankRsaKey">
        UPDATE bank
        SET key_id = #{keyId},
            public_key = #{publicKey}
        WHERE bank_code = #{bankCode}
    </update>

</mapper>