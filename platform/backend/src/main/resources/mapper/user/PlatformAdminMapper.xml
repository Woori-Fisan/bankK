<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.woorifisan.platform.user.mapper.PlatformAdminMapper">

    <select id="findById" resultType="com.woorifisan.platform.user.model.PlatformAdmin">
        SELECT * FROM platform_admin WHERE id = #{id}
    </select>

    <select id="findByLoginId" resultType="com.woorifisan.platform.user.model.PlatformAdmin">
        SELECT * FROM platform_admin WHERE login_id = #{loginId}
    </select>

</mapper>
