<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.woorifisan.platform.global.logging.mapper.SystemLogMapper">

    <insert id="insert" parameterType="com.woorifisan.platform.global.logging.model.SystemLog">
        INSERT INTO system_logs (
            created_at,
            level,
            log_type,
            trace_id,
            staff_id,
            agency_code,
            bank_code,
            target_code,
            bank_key_id,
            http_method,
            http_uri,
            http_status,
            elapsed_ms,
            client_ip,
            jws_signature,
            body_data,
            error_code,
            error_message
        ) VALUES (
            #{createdAt},
            #{level},
            #{logType},
            #{traceId},
            #{staffId},
            #{agencyCode},
            #{bankCode},
            #{targetCode},
            #{bankKeyId},
            #{httpMethod},
            #{httpUri},
            #{httpStatus},
            #{elapsedMs},
            #{clientIp},
            #{jwsSignature},
            #{bodyData},
            #{errorCode},
            #{errorMessage}
        )
    </insert>

</mapper>
